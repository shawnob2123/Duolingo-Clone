import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
   optionImage: {
    width: '100%',
    flex: 1,
  },
    optionContainer: {
    borderWidth: 4,
    borderBottomWidth: 4,
    borderColor: 'lightgray',
    borderRadius: 20,
    // size
    width: '48%',
    height:'48%',
    alignItems:'center',
    //spacing
    padding:10
  },
  optionText: {
    
  }
})